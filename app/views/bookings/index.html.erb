<h1>TES RESERVATIONS JFT</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Banner url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bookings.each do |booking| %>
      <tr>
        <td><%= booking.potager.name %></td>
        <% if booking.potager.photo.attached? %>
          <td><%= cl_image_tag booking.potager.photo.key %></td>
        <% end %>
        <td><%= link_to 'Show', potager_path(booking.potager) %></td>
        <td><%= link_to 'Destroy', potager_path(booking.potager), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
