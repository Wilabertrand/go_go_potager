<div class='background-img'>
  <div class="banner">
    <div class="container">
      <div class="pb-5">
        <h1 class="titre">Go Go Potager !</h1>
        <h2 class="titre">Le plaisir de cultiver</h2>
        <%= link_to 'Découvrez nos potagers', potagers_path, class:"btn btn-grad" %>
      </div>
      <div class="pb-2">
        <%= simple_form_for :search, url: potagers_path, method: :get do %>
        <div class="search-form-control form-group">
          <%= text_field_tag :query, params[:query], class: "form-control string required", placeholder: "Où plantez-vous ?" %>
            <%# <i class="fas fa-search"></i> %>
            <%= submit_tag "Rechercher", class: "btn btn-grad"%>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
<div class="container mt-5">
  <h3 class="intro">Vous avez la main verte mais pas de jardin ?<br>
  Vous avez un jardin mais pas le temps de le cultiver ?<br>
  Bienvenue !</h3>


  <div class="content py-5">
    <div class="potager-grid" style="display: flex; width: 100%; justify-content: space-between">
      <% @potagers.each do |potager| %>
        <%= link_to potager, style: "width: 32%;" do %>
          <div class="card-trip">
            <% if potager.photos.present? && potager.photos.attached? %>
              <%= cl_image_tag potager.photos.first.key, class:"card-img" %>
            <% else %>
            <%= image_tag 'jardins-partages.jpg', class:"card-img" %>
            <% end %>
            <div class="card-trip-infos">
              <h2><%= potager.name %></h2>
              <p><%= potager.surface %></p>
              <p><%= potager.description.truncate(30)%>
              <h2 class="card-trip-pricing"><%= potager.price %>€</h2>
              <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered"/>
            </div>
          </div>
        <%end%>
      <% end %>
    </div>
  </div>



<%# <div class="fixed-action-btn smooth-scroll" style> == $0
  <a id="scrollToTop" href="#header" class="btn-floating btn-lg wavev-effect rgba-black-strong" style="display: inline-block;">
    <i class="fa fa-arrow-up">
      ::before
    </i>
  </a>
</div> %>

